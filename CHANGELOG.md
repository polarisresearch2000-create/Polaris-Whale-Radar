# Polaris Radar 更新日志 (Changelog)

> 每次迭代升级在此记录并更新版本号。当前版本同步显示在：
> 启动脚本横幅（`启动世界杯雷达.bat`）+ bot 启动日志（`bot.js` 的 `VERSION`）。

## V9.7 — 2026-07-07（🤝跨钱包分歧:账户弃权、钱包层照记(方案C)）
- 用户发现同一场两个候选押相反方向(0x076d押Noskova@42 vs Dingwin押Keys@59, 合计101¢)。账户持双边=稀释敞口+双付点差, 期望≈-1%的糊涂仓。
  - **方案C·分层处理**: `xconfKeys`(动态计算,不落盘)——同场×同盘类出现**不同钱包不同方向** → 🤝分歧:
    - **组合层弃权**: `strengthStats`(样本外判定)与 `strengthPaper`(账户仓位/复利/流水)剔除整组(理性跟单者遇分歧不下);
    - **钱包层照记**: `strengthGroupStats`(退役规则/各钱包ROI+CLV)保留双方——同场对决是最干净的A/B样本, 谁赢谁加分。
  - 显示: 账户摘要「🤝N分歧棄權」(CLI/TG/仪表盘), 信号表 🤝 标签(瞬态`_xconf`,不写文件)。首跑抓到 **11 注**分歧——候选间互相对赌的频率本身就是"聪明钱无共识"的量化证据。

## V9.6 — 2026-07-07（僵尸信号清理:开赛超48h未结算=作废）
- 取消/延期的比赛信号会永远挂着占敞口+每轮白耗 API。新规则:**开赛已过 `VOID_HOURS`(默48h) 仍未结算 → 标 🕳️作廢**,剔出账户(`followable` 加 `!s.void`)、停止查询。计数入 `--paper`/2h推送/仪表盘摘要,信号表标 🕳️ 标签。合成测试通过(72h 前开赛的注被正确作废并剔除)。

## V9.5 — 2026-07-07（仓位改等注2%(Kelly降为对照) + 持仓按开赛排序/可切换）
- **仓位设计重审(用户问"¼Kelly是否最优")——诚实结论:不是。** Kelly 只在 edge 已知且为正时最优;当前样本外 edge 为负,严格 Kelly 最优解=0。测量期的正确姿势是**等注(flat staking,业界验证edge的标准)**:每信号投本金固定比例,每注对结论贡献相等,不被"假设edge"的价格加权扭曲(V9.1 已见 Kelly 高配低赔注的害处)。
  - **主账户 = 等注 `PAPER_STAKE_FRAC`(默2%)**+保留每场5%上限;**¼Kelly 降级为对照线**并排显示(同一信号流水两种口径实时算,零数据损失)。首测: 等注 $912(-9%) vs ¼Kelly $889(-11%)。低赔封顶不再作用于主账户(等注天然不过配),保留 ⚠️低賠 标签与 Kelly对照内的封顶。真Kelly的出场时机=某组合过四道闸门后按**实测**edge配仓。
  - **持仓排序**: `positions` 默认按 ⏰开赛时间升序(最快开赛在前);仪表盘持仓表加「⏰最快開賽 / 💰注額最大」切换按钮(前端排序,F5后回默认)+新增「開賽(HKT)」列;Telegram 2h推送与 `--paper` 均改为最快开赛在前并显示⏰。

## V9.4 — 2026-07-06（纸面账户推送 4h→2h）
- `PAPER_PUSH_MIN` 默认 240→**120** 分钟(用户要求),推送落款「每Nh更新」改为按环境变量动态显示。

## V9.3 — 2026-07-06（扩候选漏斗 + 🤖做市预过滤）
- 用户判断"地址不够看" → 扩漏斗不降标准:
  - **① 降门槛扩扫描**: `WINNER_MIN_PNL` $100k→$50k、`WINNER_MIN_BET` $2000→$1000(.bat), 扫描上限 60→`WINNER_SCAN_MAX`(默100)。理由:中型赢家($50k~$500k)做市浓度更低、更可能是真方向玩家;名单缓存带 minPnl 校验会自动重建。候选门槛(n≥15双正)/四道闸门/退役规则**全部不动**。
  - **② 🤖做市预过滤**(`applyMMRule`, 预承诺单向): 地址被捕捉 ≥`MM_MIN_N`(默8) 注中**对冲占比 ≥`MM_HEDGE_RATIO`(默60%)** → 判为做市/梯子型, 永久停跟(存 `track.mm`, 捕捉直接跳过该地址)。仪表盘「🚫停跟名單」并列 🚫双负退役 与 🤖做市; `--paper`/4h推送同步显示。
  - **首跑战果(诚实)**: 🤖抓了 2 个 —— GoalLineGhost(14/15=93%对冲) **和 swisstony(21/34=62%)**。是的, $13.3M 最大鲸鱼被预承诺规则判为做市型停跟——他也是账上唯一正收益组(勝負+$7.9)的主人, 但规则就是规则: 62%的注在双向挂 = 他的"方向腿"不可信。这反而验证了"越大越可能是MM"的判断。
  - 样本外读数更新: 15 注结算 ROI -44% 命中47%(❌, 仍是同批世界杯+网球日, 继续攒)。
- ⚠️ 多重比较提醒(已写入): 池子扩大后, 纯运气也会冒出"✅双正"组合; 上真钱前的最终门槛(跨行情+更大样本)必须更严, 不随池子放水。

## V9.2 — 2026-07-06（退役规则 + 账户流水/盈亏榜/余额曲线）
- 用户问"能否让账户自我学习/过滤地址"。诚实结论:**自适应换仓/追热手 = 把噪声当信号自动化(n=1~4的样本上"学习"就是赌运气),拒绝**;可做的是**预承诺的单向止损纪律**:
  - **🚫 退役规则**(`applyRetireRule`,纪律非学习): 地址×盘类 样本外已结算 **n≥`RETIRE_MIN_N`(默10) 且 ROI<0 且 CLV不为正 → 永久退役**(捕捉跳过、只退不进、阈值不改)。存 `track.retired`,digest 每轮结算后自动跑。
  - **📜 历史透明化**:`simulateKelly` 记流水 trail(每笔 注$/盈亏$/结后余额);`strengthPaper` 出 `history`(流水)/`board`(地址×盘类盈亏榜,亏的在前)/`retired`。
  - 仪表盘账户卡加:📈余额曲线(内联SVG,$1000基准虚线)/🏦盈亏榜/📜账户流水(15笔)/🚫退役名单。`--paper` CLI 同款;4h 推送加「最近结算4笔+退役一行」。
  - 首日盈亏榜(诚实): swisstony大小球 3注 **-$40**(最大亏源,CLV-55——历史CLV仍带修④前污染)、riverskew勝負 -$14.5、0x076d勝負 -$5.2;唯一正: swisstony勝負 +$7.9。暂无组合达到退役门槛(均 n<10)。

## V9.1 — 2026-07-06（首日前向数据复盘 → 修4个度量问题:对冲对消/每场限额/低赔封顶/CLV纯度）
- 纸面账户首日 20 注结算(-9%)的复盘照出结构性问题,全部修正:
  - **修① 对冲对消**：swisstony 在同一场大小球上双向多档(葡西一场10注 Over+Under 都押)=**做市/梯子交易,不是方向信号**。`markHedges`:同钱包×同场(`gameKeyOf`=slug截到日期)×同盘类 出现≥2个方向 → 整组标 `hedged` 剔出账户/战绩(UI 标 ⚖️對沖)。实测 35/58 注被剔(!),账户 -9%→-5%。亮灯推送也剔除已成对冲的。
  - **修② 每场限额**：84%在押集中在同场簇。`simulateKelly`/持仓配仓加 `capFrac`(默 `EVENT_CAP_FRAC`=5%)×`keyOf`(场级键):同场累计仓位≤本金5%,超出的信号仓位=0。
  - **修③ 低赔仓位封顶**：固定edge Kelly 在高价盘吃满10%上限(≈9倍于冷门注),而 ≥80¢ 收息注前向实测 -26% → `kellyFraction` 对 p≥0.80 封顶 `LOWODDS_MAXFRAC`(默2%)。等权 +2% vs Kelly加权 -9% 的差距全来自这里。
  - **修④ CLV 纯度(重要)**：`last`(收盘价基准)原在**开赛后仍刷新**,赛中价格漂向结果(输→0/赢→100)→ CLV 变成"结果回声"不是收盘线。strength 与 **记分卡** 两处都改为**只在赛前刷新**。⚠️ 记分卡历史 CLV(候选筛选标准之一)受此污染,解读时要打折;新结算起干净。
- 清洗后首日样本外读数(诚实记录): 12 注结算 ROI -30%(❌早期负信号,但仅一天/同批世界杯赛,别当定论);账户 $948(-5%),9 注计仓(3注零仓位=98¢或超场限)。

## V9.0 — 2026-07-05（成交闸门:亮灯即查订单簿,跟不进的标死+剔出账户 + 成本感知入场）
- 建**成交闸门**(四道闸门最后一块,也补上 ROI 路线第2步"成本感知"):信号亮灯时**自动查 clob 订单簿**(点差/深度/能成交价)。
  - `radar.marketExecQuote(gammaId,outcome,$)`:按目标额(默 `GATE_TEST_USD`=$50)算 最优买价/点差/吃单VWAP/深度够不够。
  - 捕捉时判 `fillable = 深度够 且 点差≤SPREAD_MAX_CENTS(默5¢) 且 能成交价<98¢`。**能跟的按真实成交价(VWAP)当入场价**(成本感知,通常比盘口中价贵一点点,ROI 更诚实);**跟不进的标 ⛔ 并从 `strengthPaper`/`strengthStats` 剔除**(你根本吃不进的单不该算进账户)。查不到盘口=不判死。
  - 显示:仪表盘/置顶/4h推送 信号带 ⛔跟不進(点差X¢) 标签、账户摘要加「⛔ N 注跟不进(已剔除)」。实测:当前信号都在流动盘(点差≤1¢)→全部✓可跟;合成薄盘信号被正确剔除+标死。
  - ⚠️ 本次**重置了 strength_track**(0 已结算=无损失)让账户统一用成本感知入场;起算时间重置为今日,进行中信号会随雷达重新捕捉。
- 至此**四道闸门全齐**:①样本外ROI+CLV(前向验证器) ②跟得住(赢家离场检测) ③成交得起(本闸门) ④跨行情(等世界杯后)。真钱前的测量基建封版。

## V8.9 — 2026-07-05（$1000 前向纸面账户:每4h推 Telegram）
- 用户要:纸面账户每隔4小时推一次到 Telegram。
  - `fmtPaperTG(track)`:HTML 版账户摘要(本金→现值·ROI·胜率·回撤·在押·浮盈 + 目前在押明细:纸面注$·地址·擅长盘·方向·成本→现价·浮盈·⚠️低賠/离场标)。
  - digest 循环加节流推送:每 `PAPER_PUSH_MIN`(默240=4h)推一次(`PAPER_PUSH_ENABLED` 默on,空账户不推只记时间)。**只读**(用运行中雷达已刷新的 strength_track,不抢写)。
  - `node bot.js --paper --push` 手动推一条;`--paper` 仍默认只读渲染。已实测推送到 DM 成功。

## V8.8 — 2026-07-05（纸面账户审计:修 4 个准确性问题）
- 详细审计 $1000 前向纸面账户的结算/离场累计逻辑。**核心账目经合成数据实测正确**(赛果赢/输 + 赢家离场跟卖平仓,终值/胜率/在押/浮盈/离场计数全对)。修掉 4 个不影响大方向但降低精度的问题:
  - **#1 复利顺序**：`strengthPaper` 原按 `entryTs`(捕捉时间)排序复利,改按**实现时间**(赛果=`kickoffMs`、跟卖=`exit.ts`)排序 → 更贴近资金真实到账顺序。
  - **#2 离场误判**：离场检测原对无 cid 信号只按方向松匹配(会把别的同向盘的卖出误判成离场)。改为**无 cid 直接跳过**、有 cid 才严格 `cid+outcome` 匹配。
  - **#3 CLV 稀释**：跟卖平仓的信号没持有到收盘,其 `clv`(≈0)原被算进样本外均 CLV。改为 `strengthStats` 均 CLV **只计 `settledBy==="result"`**。
  - **#4 抢写文件**：`--paper` 原会调用 `trackStrengthSignals`(写文件),和运行中的雷达并发写 `strength_track.json` 有丢数据风险。改为 `--paper` **默认只读**(仅渲染);要强制刷新加 `--refresh`(仅雷达没开时用)。
  - 实测:修后合成数据(捕捉序 a,b,c 但实现序 b,c,a)复利终值 $999.08 正确;CLV 正确排除离场项(+1.5pt);`--paper` 不再改文件;`--strength` 照常捕捉。

## V8.7 — 2026-07-05（子盘/低赔标注：消除"点开价格对不上"的困惑）
- 用户困惑:持仓「Will Argentina vs Egypt end in a draw · No · 80¢」点开却是 Argentina 72¢。—— 因为那是**子盘(是不是平局)**,连结落到赛事页默认显示**胜负盘**(阿根廷72¢),而你的仓在"平局?"这个子盘上(No=不平局=79.5¢,平局=20.5¢)。实测 Polymarket **子盘 slug 不能直接深链**(只有赛事页 slug 有效),所以改用标注而非改链:
  - `getMarketNow` 现返回市场 `slug`;`trackStrengthSignals` 每轮存 `sg.marketSlug`。仪表盘持仓/信号表:市场 slug≠赛事 slug → 标 **「子盤」**(提示:点开落在赛事页,往下找此盘才是此价);
  - 入场价 ≥80¢ → 标 **「⚠️低賠」**(价高=收息型,上档小、输一次抹多次)。此例阿根廷 No@80¢、Elfsborg O/U@95¢ 都被正确标出。
  - 意义:一眼看出哪些亮灯是"子盘/高价低赔"的弱信号,别被"匹配了擅长盘"误导——匹配≠好赔率。

## V8.6 — 2026-07-05（前向纸面账户显示"当前在押明细"：跟进哪些盘 + 纸面注 + 浮盈）
- 用户要：$1000 账户里也要看到"我在模拟跟进哪些盘口"。—— `strengthPaper` 现在返回 `positions`(每个进行中信号: 纸面注=按当前本金¼Kelly定 + 浮盈=(现价−成本)/成本×注)。
  - 仪表盘账户卡下加「📌 目前在押明細」表:纸面注$ · 地址 · 擅长盘 · 项目(可点) · 方向 · 成本→现价 · 浮盈 · 状态(持仓中/⚠️赢家已卖,离场行标红)。汇总行加 · 浮盈 合计。
  - `--paper` 命令行同款明细列表。`strengthPaper` 加 `positions/unrealTotal`。

## V8.5 — 2026-07-05（赢家中途离场检测：亮红灯 + 跟卖平仓 + 记录）
- 用户担心："赢家可能中途卖出,信号失效,我希望程序记录并让我知道"。—— 对,跟单必须**连出场一起跟**,否则赢家跑了你还死抱。
  - **检测**：`walletActivity` 现在也返回该地址的 **SELL 记录**;`trackStrengthSignals` 对每个未结算亮灯信号,查该赢家在 `entryTs` 之后有没有卖出同一 `cid+outcome`。捕捉时存 `cid`+`winUsd`(赢家买入额)以算卖出比例。
  - **记录**：命中则在信号上记 `exit={ts,price,usd,pct}`;`pct≥60%`(或额大)=基本离场,否则=`reduced`减仓仍持有。
  - **跟卖平仓**：赢家基本离场且比赛未结算 → 纸面账户**按现价跟着卖出平仓**(`settledBy="exit"`, profit=(现价−成本)/成本);`simulateKelly`/`strengthPaper` 改用已存 `profit`,正确计入这类提前平仓的盈亏。
  - **让你知道**：① Telegram **离场警报**(`fmtStrengthExitAlert`,红灯🔴,digest 每轮有新离场就推);② 仪表盘进行中表里离场信号**整行标红** + 状态列「⚠️贏家減倉N%/已賣」,已结算表里跟卖的标「⚠️跟賣 ±X%」;③ 顶部战绩加「⚠️贏家離場:跟賣N·減倉M」;④ 置顶同款。`strengthStats` 加 `exitN/reducedN`。
  - 意义:这才是"跟得住"的关键风险——若赢家**频繁中途离场**,说明照抄入场、抱到结算的策略本身就错,这套会用数据把它照出来。

## V8.4 — 2026-07-05（仪表盘本地服务模式：浏览器 F5 即刷新,无需重启）
- 用户问"是否要重启才刷新、能不能直接 F5"。原来 `dashboard.html` 是静态文件,F5 只重读文件,文件只在雷达每轮/双击.bat 时才重写。
  - 新增 `node bot.js --serve [端口]`(默8899)：起一个零依赖本地 HTTP 服务,**每次请求(F5)都用当前数据文件现算并重渲染**(不联网,秒回) → 浏览器 F5 即见最新(无需重启、无需重跑.bat)。起服务后自动打开浏览器。`buildDashboard` 现返回 `html` 供直接服务。
  - 「打开仪表盘.bat」改成启动该服务(窗口保持打开=服务在跑,关窗口=停)。
  - 说明:F5 拿到的新鲜度 = 雷达上轮写入数据的新鲜度(记分卡/亮灯/現價 每轮由运行中的雷达刷新);要连 /activity 实时拉,仍走 `--dashboard --refresh`。

## V8.3 — 2026-07-05（$1000 账户从「样本内回放」改成「前向纸面账户」）
- 用户要点：**别回测,要前向** —— "若赢家出手,我也跟单同样策略"。把仪表盘的 $1000 Kelly 账户从回放候选历史注,改成**只跟擅长盘·前向验证器亮灯信号**(样本外·按你能成交价·真赛果结算)。
  - `strengthPaper(track)`：起始$1000,只吃 `strength_track.json` 里 afterFreeze 的亮灯信号,¼Kelly(复用同一 `kellyFraction` 引擎,固定假设edge,无 look-ahead),已结算按赛果复利,未结算算在押敞口。出 现值/ROI/胜率/最大回撤/进行中注数+敞口。
  - 仪表盘「🎲 $1000 前向纸面账户」板块**取代**原样本内回放表。`node bot.js --paper` 查看(先跑一轮捕捉/结算)。`--simulate` 保留但标注【样本内回放·仅参考】并指向 --paper。
  - 现状：$1000→$1000(0 已结算),进行中 15 注、在押 ~$348 —— 起算干净,等结算攒真前向战绩。
  - 诚实升级：这才是"跟不跟得赚"的诚实答案(不是回放,无选择偏差,无 look-ahead);仍非投注建议、未证明 edge、需攒几周。
- 重构：抽出 `kellyFraction(p,opts)` 供 simulateKelly 与 strengthPaper 共用。

## V8.2 — 2026-07-05（进行中亮灯信号加「現價」列）
- 仪表盘「🔦进行中的亮灯信号」表加 **現價** 列(在 成本 与 CLV 之间):看候选出手后价格有没有跑 → 现在跟还跟不跟得上。相对成本涨=红(跟更贵)/跌=绿(更便宜)/持平=灰,变动≥2¢标出 ±幅度。
- `trackStrengthSignals` 现在**每轮给所有未结算信号刷现价**(`nowPrice`/`nowTs`, 一次 `getMarketNow` 兼刷现价+临近开赛的 last(CLV基准)+结算)。`--dashboard --refresh`(打开仪表盘.bat) 也顺带跑一遍捕捉+刷现价。结算后不显示現價(市场已关)。

## V8.1 — 2026-07-05（擅长盘·前向验证器 + 实时匹配"亮灯"信号）
- 把"聚焦擅长盘"从【回放结论】变成【前向信号 + 样本外验证】。一件事三作用：
  - **① 实时匹配亮灯**：`trackStrengthSignals` 盯候选,只有当他们的**新赛前出手落在自己【已冻结】的擅长盘**时才捕捉(swisstony 押大小球→亮;押胜负→忽略)。非擅长盘噪声全过滤。新亮灯每轮 digest 推 Telegram(`fmtStrengthAlert`)。
  - **② 纸面记账 + 前向 ROI/CLV**：捕捉时按**你能成交的价**(mktPrice)锁定 → 临近开赛刷新 last 算 CLV → `getMarketNow` 结算。存 `data/strength_track.json`。
  - **③ 样本外验证**：擅长盘标签**首次达标即冻结**(记 `since`),战绩**只统计冻结后捕捉的信号**(`afterFreeze`)。`sgVerdict`:样本外 ROI+CLV 双正=✅edge站得住 / 转负=❌之前是选择偏差别再跟。
  - 出口：`node bot.js --strength [--dry]`、仪表盘顶部「🏅只跟擅长盘·样本外前向验证器」板块(战绩+进行中亮灯表+已结算表)、Telegram 置顶(`postOrUpdateStrengthPin`,`strengthPinId` 入 `--repin` 顺序)、digest 每轮自动捕捉/结算/推送。
  - 首跑即捕捉 11 条在擅长盘的赛前信号(riverskew→胜负、swisstony→大小球、GoalLineGhost→是非盘),等结算攒样本外战绩。
  - `betKind` 再收紧:半场/单节/单节 O/U(1st Half O/U 等)归衍生,不算核心大小球。`data/strength_track.json` gitignore。
- ⏭️ 验证够样本(≥10 已结算样本外)后要提醒用户的下一步：①盘口专家名册(按类型汇池) ②专家共识(2+同类同向🔥) ③成交闸门(--quote) ④擅长盘专属Kelly。

## V8.0 — 2026-07-05（擅长盘识别 + 高信心组合 + $1000 Kelly 模拟账户）
- 用户要「分析候选地址各自擅长什么盘、在仪表盘强化这些高胜率信号、加个 $1000 Kelly 模拟账户算胜率」。三块：
  - **① 擅长盘识别**：`walletProfile` 新增 `strengths` —— 从盘口画像里挑出该地址 n≥`STRENGTH_MIN_N`(默6)、ROI+CLV 双正、且非「衍生/散户」的盘口类型(得分=ROI+CLV 排序，n<15 标 tentative)。`--profile` 末尾加「🏅擅长盘口」行；仪表盘候选卡加绿色 pill；近期出手明细里属擅长盘的行**高亮+🏅**。
  - **② 🏅高信心跟单组合**（仪表盘顶部新板块）：把所有候选的「地址×擅长盘」拍平、按得分排 → 你真正只该盯的那几条(GoalLineGh 是非盘16场75%/+43%、swisstony 大小球22场+30%、riverskew 胜负/让球21场67%/+14%…)。落款：**只跟「地址×他擅长的那类盘」，别笼统跟人**。
  - **③ 🎲 $1000 Kelly 模拟账户**（`--simulate` + 仪表盘板块）：回放候选地址已追踪信号，起始$1000复利，对比 固定2% / ¼Kelly / ½Kelly / ¼Kelly只跟擅长盘，出 终值/ROI/胜率/**最大回撤**。
    - **⚠️ 修了一个致命 look-ahead**：初版用每注收盘价 `last` 当真概率估 Kelly 仓位 → 因已结算注的 last 已漂向赢家，等于"照着赛果下注"，跑出胜率97%/ROI+150万% 的假象。改为**固定保守 edge 假设**(`SIM_EDGE` 默3¢，仓位与赛果无关)。修正后：胜率恒 63%(仓位不改变谁赢)、½Kelly 回报高但回撤翻倍(35% vs 21%)、**只跟擅长盘 胜率更高(67%)且回撤更低(12%)** —— 数据自证「聚焦擅长盘」更优。
    - 诚实基调：这是**样本内回放**(候选=因赚过才被选中，必然乐观)，用途是看仓位纪律与回撤，不是收益承诺。GIGO —— 未证明 edge 前 Kelly 不创造钱。

## V7.9 — 2026-07-05（值得跟进地址的「近期出手明细」：项目/成本/现价/状态）
- 用户要「值得跟进的有实力地址、他们详细出手：什么项目下注、下注成本等」。
  - **`radar.walletActivity(addr)`**：拉该地址 `/activity`，取近14天(默)方向性 BUY(price 0.1~1、≥$500、体育)，每笔富化 当前盘口价 + 是否完赛/开赛 → {title, outcome, price(成本¢), usd(成本$), ts, mktPrice(现价), status(可跟/進行中/已結算)}。
  - **`--profile <地址>` 末尾附「近期出手明细」**：逐笔 時間 · 項目 · 押哪邊 · 成本¢($) · 現價 · 狀態。首測 riverskew 就照出近期滑坡的來源=**角球O/U連輸**(Paraguay-France 9.5 Corners 押 Under 多筆全 0¢)，而 MLB 勝負盤仍贏。
  - **仪表盘每个候选卡下加「🔎近期出手明细」表**(項目可點跳 Polymarket)。数据存 `data/wallet_detail.json`(gitignore)，雷达每轮 digest 自动刷新；`--dashboard --refresh`(「打开仪表盘.bat」已默认带)按需刷新候选明细。
  - `betKind` 修正：角球/黄牌/半场等即使是 Over/Under 也归「衍生/散戶」(之前误算进核心大小球)。
- 桌面已加「聪明钱仪表盘」快捷方式(→ 打开仪表盘.bat)。

## V7.8 — 2026-07-05（钱包深度画像 --profile + 本地仪表盘 dashboard.html）
- 用户要「研究6个候选地址的投注策略、盯它们跨行情是否站得住」+「建个仪表盘方便看」。
  - **① `node bot.js --profile <地址或名字>`**：从记分卡已存的每笔 entry 价，把一个钱包的已结算注**按入场价分桶**（押熱門≥55¢ / 五五盤 / 押冷門≤45¢）各算 ROI+CLV → **分辨順風車 vs 真本事**（只在押熱門時賺=順風車，冷門/五五盤也賺且 CLV 正=真本事）。另出 盤口畫像（勝負/大小球/讓球/衍生 各自 ROI）+ 近期滑坡（最近8場）+ 一句判定（✅真本事/🟡順風車嫌疑/❌別跟/⏳樣本不足）。`walletProfile`/`statsOf`/`betKind`/`findWallet`/`fmtProfileText`。
  - **② `node bot.js --dashboard`（或雙擊「打开仪表盘.bat」）**：生成本地 `dashboard.html`（暗色、零依賴、瀏覽器打開），把 總覽 + 每個候選的「賺從哪來」拆解卡 + 其餘足夠樣本表 + 全體育板塊戰績 + 我的台賬 一頁可視化。`--dashboard --refresh` 先跑一輪捕捉/結算再生成。雷達運行時每輪 digest 也**自動刷新** dashboard.html（隨開隨看即最新）。
  - 首測發現(誠實)：riverskew 押熱門 -2%、冷門/五五盤 +40%/CLV+21.8 = **真本事(edge 在非熱門)**，但近8場 -7% 在降溫；swisstony($13.3M) 押熱門 +0%/CLV-1.1、冷門 +73% = 連巨鯨的 edge 也只在冷門/大小球，不在熱門；phonesculp 近8場 -34%/CLV-13.7 = 明顯降溫。→ 分桶+近期窗口正是「跨行情是否站得住」的照妖鏡。
  - `dashboard.html` 已 gitignore（生成物）。

## V7.7 — 2026-07-05（全体育板块自带可信度：CLV + 每条历史ROI + 板块战绩）
- 用户问「全体育板块是否可信」——查实测(164信号)发现：跟大户胜负盘 100场 ROI**-8%**、让球-25%，跟💎胜负盘 +4%(勉强)。→ 让板块**自己把可信度写在脸上**：
  - **① 多体育追踪加 CLV**：`trackMultiSport` 结算改用 `getMarketNow`(临近开赛3h内用真实盘口价刷新收盘价),算 `clv=近开赛价−入场价`,累进 `strategies[kind][key].clvSum/clvN`。(存量164条无CLV,新结算起累积。)
  - **② 每条信号内联历史ROI**：`msVerdict(s)`→{roi,clv,emo,label}(样本≥`SHARP_MIN_N`默15才判)。板块里 胜负盘/大小球/让球 每行末尾加 `· 跟大戶歷史-8%❌`。
  - **③ 底部「📊板塊戰績」**：胜负/大小球/让球 × 跟大户/跟💎 的 场数·ROI·CLV·标签(✅雙正/🟡ROI正CLV不正/❌別跟/⏳樣本不足)。`fmtMultiSport(games, stats)`、`fmtMultiSportStats`、digest、`--sharps` 全接入。

## V7.6 — 2026-07-05（记分卡:样本量闸门 + 候选标记 + 总览）
- **让记分卡别再拿小样本噪声当信号**(用户反馈榜首 +337% 其实只 2 场):
  - `SCORECARD_MIN_N`(默15)样本量闸门。`scorecardRows` 加 `enough`(n≥MIN)/`candidate`(enough 且 ROI+CLV 双正) 标记,**按 候选→足够样本→小样本 分层排序**(不再按 ROI 把噪声顶上来)。
  - 新增 `scorecardOverall(sc)`:跟所有💎信号的**合计** ROI/命中/CLV → 置顶顶部一行「📊 總覽」。
  - `fmtScorecard` 重构:总览 → ✅候选可跟(≥MIN 且双正) → 其余足够样本(参考) → 「另有N个样本<MIN(噪声,已隐藏)」。`--scorecard` CLI 同款。
  - 实测(400注): 总览 命中58% ROI+7% CLV+3.1pt;6个候选(riverskew 23场+30%/+15.7pt、0x076daa 19场+31%/+10.4pt、phonesculp 21场+20%/+12.5pt 最强);最大鲸鱼 swisstony$13.3M 44场仅+2%/CLV-0.9pt(未双正)。**仍是同一批世界杯窗口,有选择偏差,未跨行情=未定论。**

## V7.5 — 2026-07-03
- **赛果总结(🏁 賽果總結)每场加上大小球 + 让球结果**(用户要求)。原本只有胜负盘;现每场额外显示 `⚽ 大小球: 進N球→大/小球 · 大戶偏大/小 ✅/❌ · 💎偏大/小` 与 `⚖️ 讓球(-1.5): 大戶偏讓球方/受讓方(實際讓過/沒讓過) ✅/❌`(数据来自 settled 记录的 `ou`/`spread` 字段,无则跳过)。

## V7.4 — 2026-07-03（修结算漏洞）
- **修 bug:已完赛但结算不到(赛果不更新)** —— 用户发现 Portugal/Spain/Switzerland 打完了置顶战绩还是旧的。根因:ESPN `fifa.world/scoreboard` 默认接口是**滚动窗口**(只给未来场),比赛打完很快滚出去;而结算靠遍历该接口找已完赛场 → 滚出去的永远结算不到(尤其电脑关机时打完的场)。
- 修:`getWcResults` 除默认接口外,再按**最近4天 UTC 日期**各拉一遍 `?dates=YYYYMMDD`,合并去重(已完赛版本优先)。验证:现在返回12场,Portugal/Spain/Switzerland 3场已完赛全部捞回。顺带 null-guard 了 DK 赔率解析(完赛场 odds 结构不同)。

## V7.3 — 2026-07-03（赢家出手:标出收息/散户盘,突出真方向性）
- **给「赢家最新出手」加质量分级**(用户想跟"近乎必然"的高胜率盘,纠正:高胜率≠盈利,胜率已写进价里,输一次抹掉九次赢):
  - `betClass(b)`:近乎必然(价≥85¢)标 `⚠️收息·高勝率≠盈利`;衍生/散户盘(exact score/BTTS/team to score/advance/corner/halftime/player/set…)标 `⚠️散戶盤`;胜负/大小球/让球=核心方向性,不标。
  - 每个体育组:**核心方向性排前**(占 `WINNER_PER_SPORT` 名额)+ 少量带⚠️的散户/收息盘仍显示(`WINNER_FLAG_SHOW` 默2)→ 你想看还能看,但一眼知道是陷阱不是信号。
  - 落款改成「真賺看 ROI/CLV(記分卡),不是勝率」。

## V7.2 — 2026-07-01（DraftKings 差价信号）
- **DK 差价信号(最高 EV 的下注角度)** —— 把 Polymarket 隐含概率 vs **DraftKings 无水位公平价**对比,买在 Polymarket 比博彩公平价便宜 ≥`DK_MIN_GAP`(默4pt) 的一侧 = 理论 +EV。数据现成:`getWcResults` 从 ESPN 解析 DK 赔率(`o.moneyline.{home,draw,away}.close.odds` + `o.total`,美式→隐含→去水位)。
  - `radar.dkEdges(wc, pmEvents)`:每场算三方无水位公平价 vs Polymarket Yes 价(轻量,不拉成交),取便宜最多的一侧。
  - `bot.fmtDkEdges` + `trackDkEdges`(赛前锁定 value 侧+PM入场价 → ESPN 结果结算 → ROI)。`node bot.js --dk [--dry]` 看全部对比+信号+前向战绩;`trackResults` 每轮捕捉/结算,有信号节流推(`DK_MIN` 默6h)。存 `data/dk_edges.json`。
  - **⚠️ 诚实首测结果:世界杯顶级盘口 Polymarket ≈ DraftKings(差价≤1.5pt,0 个≥4pt 信号)= 市场很有效,这个角度在热门盘上没 edge。** 印证立项结论。价值在于**持续监控冷门/薄盘的偶发错价** + 前向验证那些错价到底赚不赚(大概率也≈0)。可扩到 O/U/让球、其它运动(各有 ESPN DK 赔率)。

## V7.1 — 2026-07-01（置顶优化）
- **🐛 修重复置顶 bug** —— `editMsg` 把 Telegram "message is not modified"(内容没变)当成失败 → 调用方会重发一条新置顶。现识别该错误当**成功**(return true),不再重发。所有置顶受益。
- **🗑️ 退休置顶③"今日赛果"** —— 领头是纯"命中 X/Y"(与"命中率≠盈利"理念冲突)+ 与①②重复。`postOrUpdateResultsPin` 改为一次性 `unpinChatMessage` 旧置顶并清 id,之后 no-op。赛果 ROI 仍在①策略战绩看。
- **📒 新增"我的下注台账"置顶** —— `fmtLedger`/`postOrUpdateLedgerPin`,把你真实下注的已实现盈亏/ROI/胜率放进 DM 置顶(空台账不置顶),随赢家出手 digest 刷新。`ledgerPinId` 存 digest。
- **🔀 置顶顺序 `--repin`** —— `node bot.js --repin`:`unpinAllChatMessages` 后按 底→顶 顺序重排(①战绩→②预判→📒台账→④赢家→⑤记分卡**在最上**)。重启后手动跑一次即可,顺带清掉退休的③与任何孤儿置顶。

## V7.0 — 2026-07-01
- **② 稳定 watchlist(修"名单漂移")** —— `winnerRecentBets` 现接受 `trackedWallets`,把**记分卡里已跟踪过的地址**并进扫描集(优先且必扫),再补当周活跃赢家。→ 一个地址一旦进了记分卡就**持续跟踪、不因掉出活跃榜而断更**,每钱包样本才能收敛。扫描上限 40→60。
- **③ CLV/结算更准** —— 新增 `getMarketNow(gammaId)`(一次拉取给 closed + 各 outcome 现价 + 赢家)。记分卡对**开赛前3小时内**的未结算注,每轮用**真实盘口价刷新 last**(→ CLV 用的收盘价更接近真收盘),同一次拉取顺便结算。不再依赖"赢家出手还在24h窗口内"。
- **④ DM 去噪** —— 新增 `POSITIONING_ENABLED`(默on);世界杯 .bat 设 **off**(砍掉每小时的持仓快照消息)+ 全体育 `SHARP_MIN` 6h。焦点回到两条置顶:💎赢家最新出手 + 📇记分卡。
- **⑤ 个人下注台账(ROI路线第3步)** —— `node bot.js --log-bet <eventSlug> <outcome> <price> <stake>` 记你真实下的注(自动定位 gamma市场id);`node bot.js --ledger` 赛后自动结算 + 打印**已实现盈亏/ROI/胜率**;digest 每轮也自动结算。存 `data/my_ledger.json`。验证:记一注已解析的 MLB(Dodgers@60¢ $100)→ 结算 +$67 ROI+67%。

## V6.9 — 2026-07-01
- **每钱包前向记分卡(scorecard)** —— 回答"跟单赢家到底能不能赚"的核心工具:不是笼统"跟赢家",而是**逐地址前向记账,筛出真正值得跟的那几个**。
  - `winnerRecentBets` 现返回 `{list(去重显示), raw(逐钱包)}`,并给每笔附 `gammaId` + `mktPrice`(**当前盘口价≈跟随者能成交的价**,不是赢家的成交价 → ROI 更诚实、不偏乐观)。
  - `trackScorecard(raw)`(bot):锁定赢家**赛前**出手(只锁 kickoffMs>now,杜绝 look-ahead) → 每轮刷新最后观测价(CLV 用) → 赛后 `getMarketResolution` 结算 → 每地址算 **命中率 / ROI(按能成交价) / 均CLV**。存 `data/wallet_scorecard.json`。
  - `fmtScorecard`+置顶⑤(就地刷新),`node bot.js --scorecard` 手动看。**判据:只有 ROI 与 CLV 都持续为正的地址,才值得专门跟**(样本少前别信)。
  - 诚实基调:跟单结构性劣势大(慢一步/拿更差价/选择偏差/做市污染),大概率整体微亏,这张卡就是用来**用数据证否或证实**,而非假设能赚。

## V6.8 — 2026-07-01
- **赢家最新出手:标出开赛时间(HKT) + 跨钱包去重**。① 每场标题行加 `⏰ 開賽時間`(查 closed 时同一次拉取顺便取 startTime,零额外请求);下注时间也标成"下注N前"以区分。② 之前同一场同一方向多个赢家会重复列(例:6个赢家都押 Tommy Paul → 6行)。现在**跨钱包按(市场+方向)去重**,只留最赚的赢家一行,并标 `💎×N同押`(N=有几位赢家押同一边=共识强度),金额取其中最大注。→ 看到更多**不同场次** + 一眼看出共识。

## V6.7 — 2026-07-01
- **修 bug：赢家最新出手会把"已完赛"的场也推出来**(用户反馈:3场 06-30 的 MLB 昨天就结束了还在列)。原因:只按下注时间(4~8h前)过滤,没查那场是否还开着。修复:`winnerRecentBets` 取最近80笔后,按 `eventSlug` 查 gamma `event.closed`,**剔除已结算/已完赛的场**(只留还能下注的);已完赛=`closed:true`,未开赛=`closed:false`,验证有效。

## V6.6 — 2026-07-01
- **「💎赢家最新出手」改成自动更新的置顶**(用户要置顶这条)。原本每次发新消息(tx去重);现在 `postOrUpdateWinnerPin` **就地编辑同一条 + 置顶**(像策略战绩/即将开赛那两条置顶),`winnerPinId` 持久化在 `digest_<tag>.json`。加了「持續更新 · HH:MM HKT」时间戳行(保证每次内容不同、避免 editMessageText "not modified" 报错回退成发新消息)。`--winner-bets` 手动跑现在也是创建/刷新该置顶。每 `WINNER_MIN`(默90分, .bat 里=60分)刷新一次。

## V6.5 — 2026-07-01
- **「💎赢家最新出手」按体育分类分组** —— 之前是全体育混在一起按时间排;现在按 ⚽世界盃 / ⚾棒球(MLB) / 🎾網球 / 🏀籃球… 分节列出(`sportOf()` 用关键词 + MLB 队名兜底分类)。每节最多 `WINNER_PER_SPORT`(默6)笔、组内按时间倒序,节标题显示 `顯示6/41` 以免世界杯挤掉棒球/网球。`winnerRecentBets` 抓取上限 25→50 让各体育都有货。

## V6.4 — 2026-07-01
- **💎 赢家最新出手 feed(wallet-centric, 给"参与更多投注")** —— 之前的信号都是 market-centric(按盘子看谁在押);这条反过来:**盯一批盈利大户钱包, 推他们最近刚下的方向性注**, 按时间倒序, 覆盖全体育全盘口(胜负/大小球/让球/半场/晋级…)。
  - `buildSportsWinners()`:从各体育 tag 高量对局盘收集活跃钱包 → 按全期盈亏筛 ≥`WINNER_MIN_PNL`(默$10万) → 缓存 `data/winners_sports.json`(12h)。
  - `winnerRecentBets()`:名单里每个赢家近 `WINNER_HOURS`(默24h) 内、价 0.1~0.9(方向性)、≥`WINNER_MIN_BET`(默$2000) 的体育下注,每钱包最多3笔,按时间倒序。
  - `bot.fmtWinnerBets` + `--winner-bets [--dry]` + digest(每 `WINNER_MIN`=90分, **tx 去重只推新的**, 不重复)。
  - 实测即时抓到:$13.2M 赢家刚买 England-DR Congo Over 1.5(4分前)、$259k 赢家 Mexico-Ecuador Over $4万(2h前)等。
- **本地"更多信号"节奏**:`启动世界杯雷达.bat` 调回更勤(poll 3分、持仓1h、sharps 3h、赢家出手1h)。
- ⚠️ 诚实:**更多信号 ≠ 更多胜率**;下注越多对(未证明的)edge 暴露越大、点差/费吃越多。feed 仍标"非投注建议·未证明 edge"。

## V6.3 — 2026-07-01
- **MLB/网球 前向 ROI 追踪(全体育战绩)** —— 让非世界杯的 胜负/大小球/让球 也能像世界杯一样前向测 ROI,不只是快照。**改用 Polymarket 市场解析结算(非 ESPN)**:更 uniform、免去 ESPN 队名/球员名匹配的脆弱性。
  - `multiSportSentiment` 每场信号补上 gamma 市场 id + backed/winner outcome 索引 + 入场价(`mlId/mlBackedIdx/mlWinnerIdx` 及 `ou/spread` 的 `id/prices/sideIdx/winnerIdx`)。
  - `trackMultiSport()`(bot): ①锁定赛前信号到 `data/results_multisport.json`(**只锁"有开赛时间且未开赛"的真赛前场**,杜绝锁到已解析/in-play 盘造成假 ROI);②开赛后对每个市场 `getMarketResolution(id)` 拿赢家 outcome,按下注价算 `跟💎赢家 / 跟大户` 的命中率+ROI(胜负/大小球/让球 各一套)。
  - `node bot.js --sharps-results` 查看; sharps digest 每轮顺带 捕捉+结算,有新结算自动推「🎯 全体育战绩」。新增 `SHARP_TRACK_TOP`(默15,锁定几场)。
  - 端到端验证:`getMarketResolution` 在已结算 MLB 盘(Twins vs Astros → Astros)正确返回赢家 outcome,settle 逻辑正确判赢输。仍是**未证明 edge**,前向攒样本。

## V6.2 — 2026-07-01
- **成本感知 ROI(订单簿真实点差/深度 → 能成交的价 + 流动性闸门)** —— 转个人下注、上 Kelly 前的必经一步(§11 两个 ❌ 闸门的引擎):
  - `getBook(tokenId)`(clob `/book`)+ `execQuote(book, $)`:算最优买/卖价、**点差**、吃单到成交的**加权均价(VWAP)+滑点**、**深度够不够**吃满目标额;`getExecQuote(tokenId,$)` 一步到位。
  - `quoteMatch(eventSlug, $)`:对一场的 胜负盘/大小球2.5/让球-1.5 每个可下注方向都报价(用 gamma `clobTokenIds` 拿每 outcome 的 token)。
  - `node bot.js --quote <eventSlug> [美元额] [--dry]` → 「💰 成本感知報價」:每方向 买价+点差+成交均价+滑点,点差>`SPREAD_MAX_CENTS`(默5¢)或吃不满=⚠️慎入。**成本计入才是真 ROI —— 点差/滑点吃掉的就是 edge**。

## V6.1 — 2026-07-01
- **CLV 扩展到让球** —— `getClosingPrices` 现在也返回主 -1.5 让球盘的 cover/not 收盘价 + favTeam;CLV 捕捉环按 favTeam 匹配同一场,算 `clv.spread`。`clvStatsLines` 加「讓球」行 → 胜负/大小球/让球 三条都能用"买没买到好价"提前判断有没有 edge。
- **多体育(MLB/网球)也接 O/U + 让球子信号** —— 新增通用 `sideSignal(mk, minUsd)`(任意 2-outcome 盘的大户偏向 + 💎盈利大户押哪边)。`multiSportSentiment` 对要展示的 topN 场,从该场已有 markets 里找**主 O/U 盘**(成交量最高、非分盘/半场)和**主让球盘**(-1.5/让分),算子信号,显示在「🎯 近期聪明钱·全体育」每场下:`⚽ 大小球: 大戶偏 大 76%（O/U 7.5）` / `⚖️ 讓球: 大戶偏 X +1.5 81%`(带💎赢家✓/⚠️分歧)。子盘太薄自动省略(网球衍生盘多半太薄;MLB 有量的对局齐全)。
- 窗口默认 14→**21天**(MLB 有量含 O/U/让球 的对局多在2~3周内);`SHARP_WINDOW_H` 默认同步 504。实测 Tampa-Boston:大户偏大球76%(💎赢家同向) + 让球偏 Rays+1.5 81%(💎同向)。

## V6.0 — 2026-07-01
- **让球(spread -1.5)聪明钱信号 + 前向追踪** —— 系统分析11个赢家(PnL≥$200k)的下注按盘口类型后确认:让球是赢家第三大类(14%资金、7/11在玩,且单笔金额比大小球更大=更像重仓有观点),仅次于胜负盘/大小球。与大小球同一套纪律:
  - `getSpreadSignal(eventSlug)`:从 more-markets 取**成交量最高的 -1.5 让球盘**(标准亚盘),看大户偏"让球方赢2+球(cover)"还是"受让方+1.5(not)",返回入场价 + 💎盈利大户押哪边。
  - `capturePredictions` 存 `spread`(+回填);结算时按 `让球方净胜球>1.5` 判 cover/not,并行测 **跟💎盈利大户 / 跟大户 / 强共识≥75%** 三策略,逐场存 `spreadSettled`(供 让球方/受让方 分段 ROI)。
  - 预判(置顶②/每日)每场加 `⚖️ 讓球:` 行(大户偏向 + 💎赢家✓同向/⚠️分歧);置顶战绩 + `--results` 加「⚖️ 讓球戰績」块。
  - 实测分歧:France-Sweden 大户偏 Sweden +1.5(55%),但 $13.4M 赢家偏 France -1.5(⚠️分歧)。至此跟踪覆盖赢家 **~80% 的钱**(胜负+大小球+让球)。仍是**未证明 edge 的候选**,靠前向测量。

## V5.9 — 2026-07-01
- **「🎯 近期聪明钱 · 全体育」—— 世界杯之外的更多可分析场次**(个人自用)。需求:世界杯每天才几场,想要更多"赢家在押哪边"的场。经实测,当前 Polymarket 上有量的还有 **MLB(天天打~15场)** 与 **网球Wimbledon(每天多场)**;且各运动是各自的一批 sharp(世界杯的赢家≈只玩世界杯的扫单党)。
  - `radar.multiSportSentiment(tags, {windowMs,minNotional,topMarkets})`:扫 MLB/网球等 tag 的**对局盘**。这些是 **2-outcome 盘(队名/球员名,非世界杯的每结果 Yes/No)**,主胜负盘识别=2 outcome + 非 Yes/No + 非 Over/Under + 问题不含衍生关键词(set/handicap/o\/u/spread/inning/completed…)。聚合大户资金 + `getWalletScore` 评前6钱包 → `topWinner`(💎≥$50k)/`topWhale`(🐋最大注)。只看未来 `windowMs`(默认14天)内开赛的场(聪明钱多在赛前数天~两周布局;太近的场大钱还没进,太远的满赛季远期盘噪音大)。
  - `bot.fmtMultiSport` 复用 `smartMoneyLines` 显示每场:资金分布 + 盘口价 + 💎赢家 vs 🐋最大注 `✓同向/⚠️分歧`。
  - 推送:`node bot.js --sharps [--dry]` 手动;并接入体育 digest 循环(`SHARP_ENABLED/SHARP_SPORTS/SHARP_MIN/SHARP_WINDOW_H/SHARP_TOP`,默认 mlb,tennis · 6h · 14天窗口 · 8场)。
  - 实测立见分歧信号:Molcan/Altmaier 网球——盘口74%+最大注($967k输家)押Altmaier,但$475k赢家押Molcan(⚠️分歧);Sinner/Borges——盘口84%押Sinner,$397k赢家押冷门Borges(⚠️分歧)。

## V5.8 — 2026-07-01
- **持仓分析(📊 巨鯨持倉分析)也加上「💎顶级赢家 vs 🐋最大注 分歧」标记**(个人自用·更详细)。原先二者只显示其一(else-if),现在两条都显示并标 `✓同向 / ⚠️分歧`;最大注是输家时内联 `⚠️歷史虧損`;最大注本身就是顶级赢家则合成 `💎🐋 最大注即頂級贏家` 一行。新 `smartMoneyLines(m,label)` 体育/加密两路共用。
  - 实测即时见效:墨西哥vs厄瓜多尔——盘口74%押墨西哥、最大注($1.1M输家)押墨西哥,但 **$1.1M 顶级赢家押平局(⚠️分歧)**;葡萄牙——$14.4M赢家与$2.2M最大注同押葡萄牙(✓同向)。

## V5.7 — 2026-07-01
- **预判信息加详(个人自用版)**：每场新增「💎最大赢家 vs 🐋最大注 是否分歧」一行 + 大小球行附「💎赢家方向是否同向」。
  - `matchPrediction` 新增 `proWinner`：评前6大钱包 PnL，取历史盈利≥$5万的最大赢家押哪边(胜负盘); `capturePredictions` 锁定 + 回填老预判(只取 proWinner,不覆盖已锁定入场价)。
  - 新 `signalDetailLines(p)`：`💎最大贏家 押X（+$Yk） · 🐋最大注 押Z（$Wk） · ✓同向/⚠️分歧`，大小球行加 `💎贏家偏大/小球✓同向/⚠️分歧`。即将开赛置顶 + 今日预判共用。
  - 实测即时暴露分歧：法瑞胜负盘 💎赢家与最大注同押法国(✓)，但大小球大户偏大球82%而💎赢家偏小球(⚠️分歧)——正是个人自用要看的"聪明钱内部分歧"。

## V5.6 — 2026-07-01
- **CLV(收盘线价值)前向追踪** —— 项目转个人自用,第一步先建"有没有 edge"最快的领先指标(不必等赛果):
  - `getClosingPrices(eventSlug)`:仅抓价、零钱包分析,返回某场胜负盘 + O/U 2.5 的当前盘口价;
  - `trackResults` 新增 CLV 捕捉环:每场临近开赛(≤90 分钟)首次轮询时锁定一次"近开赛价",存入 `prediction.clv.{ml,ou}`(side/entry/close/clv);CLV = 近开赛价 − 入场价,**正=价格朝你那侧移动=你买在了好价位**;
  - 置顶战绩 + `--results` 新增「📈 CLV 收盘线价值」块:胜负盘 / 大小球 各自 均CLV(price points) + 赢线率(% beat the close);
  - 用途:连续几十场 CLV 持续为正 → 大概率真有 edge(再谈成本感知 ROI、台账、最后才分数 Kelly);CLV 为负 → 别动钱。近开赛价取"开赛前 ≤90 分钟首次观测"(云端每小时一轮的折中,基本仍是赛前价)。

## V5.5 — 2026-07-01
- **大小球分段从"只看命中率"升级为"命中率 + ROI + 均入场价"**(偏大球 / 偏小球 各一行)。原因:命中率高 ≠ 赚钱 —— 若高命中那侧入场价更贵,实际可能仍亏。`ouSettled` 逐场新增 `price`(跟大户那侧入场价),分段按下注价算 ROI,直接回答"押小球到底是正 ROI 还是其实在亏"。

## V5.4 — 2026-06-30
- **大小球(O/U 2.5)前向追踪器** —— 真正去"挖大小球含金量"(押≠赢,必须前向测量"跟着押能不能赢过市场",和胜负盘同一套诚实纪律):
  - `getTotalsSignal` 增强:除大户偏向外,新增**入场价**(overPrice/underPrice,算 ROI 用)+ **盈利大户押哪边**(winnerSide/winnerPnl,评前6大钱包、PnL≥$5万记💎);
  - 结算时按 ESPN 总进球判 Over/Under(≥3=大球),并行测 **3 条假设**:`followWinner` 跟💎盈利大户 / `followBig` 跟大户(资金多数方) / `highConsensus` 仅强共识≥75%才跟,按下注价算命中率+ROI,累计进 `ouStrategies`;逐场存 `ouSettled` 供**大球/小球分段**分析;
  - 置顶①战绩底部新增「⚽ 大小球戰績」块,`node bot.js --results` 同步显示逐场;
  - 实测发现💎盈利大户常与大户(资金多数方)**反向**(如 象牙海岸:大户偏小球、$14M赢家押大球),正是分开追踪的价值。让球 spread 仍留待后续。

## V5.3 — 2026-06-27
- **砍掉准确比分预判,换成「大小球(O/U 2.5)聪明钱偏向」**。依据:拉了 Polymarket 全期盈利榜前 12 大户(盈利$7.6M–22.1M)实测 —— 他们 ~86% 资金押**胜负盘**、~12% 押**大小球/让球**,**准确比分合计仅 $10k(0.02%,且只 1 人碰)= 纯散户**。所以:
  - 删除 `getExactScoreBoard` + 比分概率榜/比分命中率(预判、每日预判、赛果总结、置顶战绩里的 🎯比分 全部移除);
  - 新增 `getTotalsSignal(eventSlug)`:从 `{matchSlug}-more-markets` 取整场总进球 **O/U 2.5** 盘,看大户资金偏 **大球/小球**;预判里显示 `⚽ 大小球: 大戶偏 大球 86%（O/U 2.5）`。实测有真金白银(如 哥伦比亚vs葡萄牙 $10万押大球),正是聪明钱所在。
  - (让球 spread 暂未纳入,O/U 2.5 是最标准最活跃的大小球盘;后续可加。)

## V5.2 — 2026-06-27
- **新增第三条置顶「🏁 今日賽果」**:赛后把**最近一个比赛日(按 HKT 开赛日期分组)**的赛果整理成自更新置顶 —— 每场 `✅/❌ 主队 比分 客队 · 看好X ✓/✗` + 顶部「賽前看好 命中 N/M」。结算时即时更新、并每≥30分钟刷新。`fmtResultsPin()` 渲染、`postOrUpdateResultsPin()` 维护、`resultsMsgId`/`resultsUpdatedAt` 持久化。结算记录补存 `kickoffMs`(缺则用 settledAt 兜底分组)。赛果总结推送照旧保留。三条置顶分工:①过往战绩 ②即将开赛 ③今日赛果。

## V5.1 — 2026-06-24
- **开赛时间(HKT)铺到「巨鯨持倉分析」**:每场对阵名后加 `⏰ M/D HH:MM`(香港时间),取自 Polymarket 事件 `startTime`(in-play 过滤本就算过 → 零额外请求)。`marketSentiment` 把 `kickoffMs` 带到 match/market 对象,`fmtPositioning` 渲染。
- **预判置顶/每日预判的开赛时间更稳**:对仍缺 `kickoffMs` 的预判(尤其改版前捕捉、已离开 ESPN 当前赛程的),`capturePredictions` 从**已拉取的 Polymarket 事件 `startTime` 兜底回填**(零额外请求)→ 不再只依赖 ESPN 当前赛程才显示时间。

## V5.0 — 2026-06-23
- **修「賽果總結」推旧信息**:每次有新结算时,这条推送以前会①重复列最近5场(含早就推过的旧赛果)②底部重列全部策略(含 V4.1 已隐藏的 fade、且是无累计u的旧格式)。现在:
  - **只展示本次新结算的赛果**(`fmtResultSummary(res, newSettle)` → `slice(-newSettle)`),不再重复推旧场;
  - **去掉底部旧策略块**,改为一行「目前最佳策略」+「📌 完整策略戰績見置頂」—— 置顶才是详细正本,不再两处不一致;
  - 标题由「賽果總結 + 策略戰績」改为「賽果總結」。
  - (同日微调)置顶战绩去掉「⚠️ 樣本仍小…噪聲大」那行警告(空态占位保留);版本号保持 V5.0。
  - (同日微调)「📅 即將開賽 · 預判」置顶**精简成独家版**:去掉暴露抓取手法的 🐋大户明细(@价/赛前X小时),改为「⭐ 看好 X（信心N%）」+「🎯 比分熱度」;队名转中文;去掉巨鲸/大户字眼(头部、底部战绩行均改写);加「⚠️ 數據分析·非投注建議」。`fmtUpcomingPin` 重写,版本保持 V5.0。
  - (同日微调)「📅 即將開賽 · 預判」置顶**加开赛时间(HKT)**:每场对阵名后加 `⏰ M/D HH:MM`(香港时间)。捕捉时把 `kickoffMs`(来自 ESPN)存进预判并回填老预判;`koHKT()` 换算。副标题标注"開賽時間 HKT"。
  - (同日微调)「📊 巨鯨持倉分析」**同样精简**:去掉**裸钱包地址**、每行的金额/人数;每个结果只留「队名 + 占比% +（盤口X¢）」凸显落差;💎/🐋/⚠️ 用紧凑金额($8.8M/$462k,新增 `cUSD`)、不再贴地址。保留核心独家洞察(顶级赢家在押谁、最大注是不是输家)。`fmtPositioning` 双路(体育三方/加密二元)同步精简。版本保持 V5.0。

## V4.9 — 2026-06-23
- **加密模块对齐体育升级 + 修 bug + 质量纪律**:
  - **修复 V4.7 引入的 bug**:置顶战绩的「逐场明细」原假定体育字段(主队/比分/客队),加密(无比分,Yes/No)会渲染成 `undefined undefined undefined`。现兼容两路:体育出"主队 比分 客队"、加密出"市场问题 + 押是/否"。
  - **盘口价进加密持仓**(对齐 V4.6):每条 Yes/No 加 `盤口X¢`(Yes=市价, No=1-市价),与大户 money 占比并列,看落差。
  - **对冲/做市过滤进加密**(对齐 V4.5):同时押 Yes+No(集中度<80%)的钱包不当 💎/🐋。
  - **加密也上"待结算预判"自更新置顶 + 战绩定期刷新**(对齐 V4.8):`fmtUpcomingPin` 标题按体育/加密自适应。
  - **竞争性过滤(质量纪律)**:加密捕捉与持仓分析都**跳过近乎确定的废盘**(巨鲸侧价 >0.90/<0.10 不追踪;持仓只显示 Yes 价 0.12–0.88 的竞争盘)。否则会出现"比特币会不会跌到$1.5万→否 100%"这种 100%胜率但 +0 ROI 的噪声,毫无信号价值。
  - 注:exact-score / 三方合并 / in-play 过滤是体育专属(加密无比分、无开赛),不适用。

## V4.8 — 2026-06-22
- **新增第二条自更新置顶「📅 即將開賽 · 巨鯨預判」**:复用战绩置顶那套机制(就地编辑 + ≥30 分钟刷新),只列**未開賽**(ESPN `state=pre`)且有预判的场(开赛的自动掉出、新场补进),每场显示巨鲸预判方向 + 最大户押注 + 市场比分榜(最多 6 场)。
  - `fmtUpcomingPin()` 渲染;`postOrUpdatePreviewPin()` 维护;`previewMsgId`/`previewUpdatedAt` 持久化在 `results`,**云端始终编辑同一条,不会重复置顶**。每日"今日预判"推送照旧(给老粉丝通知)。
  - 置顶① 战绩(看过往) + 置顶② 即将开赛预判(看接下来),各司其职。

## V4.7 — 2026-06-22
- **置顶战绩「详细表达」升级**(`fmtTrackRecord`):
  - **逐场赛果明细**:新增「📋 近期逐場賽果」—— 最近 6 场(跟巨鲸方向)逐条列出 `✅/❌ 主队 比分 客队 · 押X @入场价 · ±Nu`,用真实 `settled` 数据,透明可查,不再只有一排 ✅❌ 点。
  - **累计 units**:每条策略加 `累計 ±Nu`(1u=一注固定本金;= paper/live 表的 PnL 列的诚实版,不用编造美元)。
  - **双视图标签**:「🐋👑 跟巨鲸/最大户(同侧 · **全部出手**)」vs「🔒 只在高共识≥85%才跟(**= 你真會下單的子集**)」—— 即 PM-NO2 那张 paper/live 表的精神(全量 vs 高把握子集)。
  - TEAMS 字典补 Czechia/South Africa 中文。
  - (诚实纪律照旧:不引入 Kelly/建议仓位;9 场仍小、警告顶在那。)

## V4.6 — 2026-06-20
- **持仓分析加「盤口价」**:三方每个结果加显示市场定价(`盤口X¢`=该结果 Yes 隐含概率,来自已拉的 `outcomePrices`,**零额外 API**),与大户money占比(`佔Y%`)并列。两者落差即信号(例:荷蘭盤口56¢ 但大户钱 93% 压荷蘭=大户远比市场乐观)。`marketSentiment` 把每盘 Yes 价挂到 `sides[outcome].price`,`fmtPositioning` 渲染。

## V4.5 — 2026-06-20
- **信号质量 #1(轻量):本场对冲/做市过滤**。💎最赚大户 / 🐋最大注 只认"有方向观点"的钱包 —— 若一个钱包的钱**分散在多个互斥结果**(主押结果占其本场总额 <80% = 对冲/套利/做市),不再当聪明钱信号。用三方合并已有的 `byOutcome` 数据算集中度,**零额外 API**。加密路径无 `directional` 字段→不受影响。
  - 局限(已记 CLAUDE.md §11):只抓"跨互斥结果分散"(三方只取 Yes 侧);**未抓**同一市场 Yes+No 对冲、及全局做市机器人(需拉钱包交易史)。门槛 `DIR_MIN=0.8`。

## V4.4 — 2026-06-20
- **修复信号质量问题 #7:持仓分析排除已开赛比赛**。`marketSentiment`(体育)之前不区分赛前/赛中,比赛开打后场上(in-play)的钱仍被算进「大戶布局」,在"提前聪明钱"招牌下属误导。现按 Polymarket `event.startTime`(开赛时间, 备用 `market.gameStartTime`)过滤:**`now >= 开赛 → 整场跳过**,只统计未开赛比赛。验证:已开赛的 Brazil vs Haiti 被正确排除,只返回未开赛场次。加密不受影响(无开赛概念)。
- (审查附带)实测确认 maker/taker **无重复计数**(data-api `/trades` 每笔一行,`takerOnly` 不影响结果);新增 CLAUDE.md 第11/12节:信号质量规则表 + 资金信号代码审查清单(7条)。

## V4.3 — 2026-06-20
- **持仓分析改成「整场三方合并」(体育)**:之前按"单个 Yes/No 市场"排序,同一场比赛会被拆成多条(尤其冷门方也有量时,如厄瓜多尔vs库拉索出现「庫拉索會贏嗎」+「厄瓜多爾會贏嗎」两条)。现在按 `eventSlug` 把同一场的 **主胜 / 平 / 客胜** 三个 Yes 盘合并成**一条整场三方分布**(显示该场总金额、主-客中文队名、各结果占比),**每场只出一条**,标题用对阵名「德國 vs 象牙海岸（日期）」。
  - 实现:`marketSentiment` 体育路径按事件分组聚合 Yes 一侧 + 大户跨三盘归并到其主押结果;**加密保持原「逐个二元市场 Yes/No」视图不变**(`m.sides` 有=三方, 无=二元)。`fmtPositioning` 双分支渲染;新增 `outLabel(outcome)` 出中文队名/平局。
  - 💎最赚大户 / 🐋最大注 / ⚠️最大注却是输家 三个标注照旧,只是现在按整场归并后选取。
  - 新增 `node bot.js --preview-now`:手动推一次「持仓分析 + 今日预判(强制, 不受 PREVIEW_HOUR/previewDay 限制)」,改版后即时看效果。捕捉逻辑抽成 `capturePredictions()` 供其复用。

## V4.2 — 2026-06-20
- **新增「準確比分」維度**：每场比赛 Polymarket 有独立的 *Exact Score* 市场(0-0/1-0/2-1…16个具体比分+其他)。
  - **赛前**:在「今日巨鲸预判」里给每场加一行**市场比分概率榜 Top3**(按隐含概率,主-客视角),如 `2-1 12% · 1-1 12% · 1-0 11%`。
  - **赛后**:赛果总结里显示`赛前比分榜 → 实际比分 + 命中标签(榜首/Top3/未中)`,并**前向累计「比分 Top3 命中率」**(同时记榜首命中数),也亮在置顶战绩里。
  - **诚实纪律**:实测准确比分盘**全是散户**(最大单仅几百刀,无巨鲸),故**只用价格=市场共识概率,不包装"聪明钱押比分"**;且明确标注"准确比分本就难中(榜首也仅~12%),此为市场热度非稳赢"。
  - 实现:`radar.getExactScoreBoard(eventSlug)` 按 `{matchSlug}-exact-score` 拉榜;`matchPrediction` 增返 `eventSlug` 供拼接;预判存 `scoreBoard`、结算存 `scoreStats{n,hit1,hit3}`。每场仅捕捉时多 1 次轻量请求。
  - **修复队名匹配缺口**:ESPN「Ivory Coast」≠ Polymarket「Côte d'Ivoire」两边无重叠 token → 整场匹配不上(胜负预判+比分榜全无)。新增 `TEAM_ALIAS_TOKENS` 别名表(只增 token 不删,不破坏既有匹配),德国vs象牙海岸已能正常预判。日后遇到类似差异往这表加一行即可。

## V4.1 — 2026-06-20
- **持仓分析去标签**：每15分钟那条的标题从「📊 巨鯨持倉分析（精華版）／（…· 錢往哪押）」精简为「📊 巨鯨持倉分析／（大戶下注的多空分布）」，去掉"精華版""錢往哪押"两个赘词。
- **置顶两条同侧策略动态合并**：「🐋 跟巨鯨多數方」与「👑 跟最大單大戶」若每场都同侧(数据雷同)→ 合并成一行「🐋👑 …（同側）」，避免橱窗里两行雷同被当凑数；**一旦某场出现分歧自动拆回两行**(分歧是真信息，不丢)。自愈，无需手动切换。

## V4.0 — 2026-06-20
- **置顶战绩更详细 + 更新更勤**：
  - 每条策略由原来一行 `N場 命中X% ROI` 扩展为：**勝負記錄(3勝1負) + 命中% + ROI%** 三个维度，再加 **均入場賠率(入场价隐含倍数, 看出押热门 vs 搏冷门) + 近期 form(最近6场 ✅/❌ 连胜连败)**；顶部显示**已结算总场数**，底部高亮**目前最佳策略**与诚实的小样本警告。(注：弃用"每注$100"的拍脑袋金额——ROI 已把盘口算进去，注码无关；改用赔率呈现盘口。)
  - **定期刷新**：原本只在「有新结算」或「每日预判」时更新置顶 → 现在每轮还会检查，**距上次更新≥30分钟就刷新一次时间戳/数据**（`trackUpdatedAt` 节流），频道访客看到的战绩永远是新鲜的。注：仍需 24h 运行(本地窗口或云端密钥)才能不漏掉比赛结算——错过结算时刻 = 那场永远入不了账。
  - **置顶只展示正向策略**：`fadeFav`(反向 fade 大众)持续垫底、对"卖铲子"频道形象无益，从置顶**隐藏**（底层仍照常追踪，赛果总结/`--results` 里还在，做诚实对照）。
  - 新增 `node bot.js --refresh-pin`：只就地刷新置顶战绩、不跑完整轮询(不会误发持仓摘要)，用于改版后立即更新已置顶消息。

## V3.9 — 2026-06-19
- 持仓精华版**主推「💎最賺大戶」**：每盘对前6大户交叉 all-time PnL，选出**历史盈利最高(>$5万)的赢家**作为主推(地址+押向+金额+历史盈利)；没有赢家才退回"最大注大户"。**大输家降级为「⚠️最大注卻是輸家」一行反向提示**，不再误当聪明钱。

## V3.8 — 2026-06-19
- **去繁琐 + 整合**：世界杯频道关掉「逐条实时信号」刷屏(`SIGNALS_ENABLED=off`)，全部整合进每15分钟的「持仓精华版」一条。
- **A 聪明钱分级**：精华版里"最大单大户"按**历史战绩**标注——交叉 all-time PnL，盈利>$5万标「💎歷史盈利$Xm」。一眼看出是真赢家还是无名大额。(B 按下注领先量切分战绩 = 待样本攒够再算)

## V3.7 — 2026-06-19
- 最大单大户细节完善：记录并展示**入场价(VWAP)、下注时间、距开赛多久下注**（赛前几小时布局 vs 临场跟风一目了然）。赛果总结/每日预判都加「🐋 最大戶押 X @价 · 賽前Y小時下注」。开赛时间取自 ESPN `date`。加密版同样记入场价+时间(无开赛概念故无领先量)。

## V3.6 — 2026-06-19
- **数据完整性**：赛果追踪预判**只在赛前(state=pre)捕捉**，杜绝赛中追涨的前视偏差（回测发现：用全程持仓会高估巨鲸"准度"）。清空旧赛果记录，干净起跑。
- 新增回测工具 `backtest-whales.js`：用历史已结算盘复盘，分段"竞争盘 0.30-0.80"+"仅赛前"+全程对照。结论：当前样本太小(仅世界杯)，无法证明巨鲸预测 edge，须靠 forward 累积。

## V3.5 — 2026-06-18
- **加密频道也上「每日巨鲸预判 + 置顶策略战绩」**：加密为 Yes/No 二元市场，预判=巨鲸押 Yes/No；**结算用 Polymarket 市场解析(UMA)**（`markets/{id}` 读 outcomePrices，1=胜）。复用同一套 4 策略 ROI + 置顶机制。注意：加密非高频盘结算较慢，战绩累积比体育慢。`trackResultsCrypto()` 走加密路径，体育走 ESPN 路径。

## V3.4 — 2026-06-18
- 固定内容节奏：每天香港时间 `PREVIEW_HOUR`(默认9点) 推一条「☀️今日巨鲸预判」（今日各场巨鲸预判方向+共识+最大户+当前最佳策略）
- 命中率置顶：用 Telegram pin 一条「📌策略战绩 Track Record」，每次结算后**就地编辑更新**(editMessageText)，新访客永远第一眼看到累计 ROI 战绩

## V3.3 — 2026-06-18
- 赛果追踪升级为**多策略 ROI 前向测试**：并行测 4 条策略（跟巨鲸多数方 / 跟最大单大户 / 高共识>85%才跟 / 反向fade大众），**按下注价算 ROI**（不只看胜率，胜率会骗人）。预测捕捉时记录每个结果的下注价；完赛后逐策略累计命中率+ROI。赛果总结改为"赛果 + 策略战绩"。`--results` 同步升级。

## V3.2 — 2026-06-18
- 持仓快照精华版去掉英文版块，**只保留中文**（专注中文受众）；纯中文后每次多显示几个盘（前6）

## V3.1 — 2026-06-18
- 持仓快照副标题去掉门槛显示（不再对外显示 "統計 ≥ $X 的買入"）
- 世界杯频道关闭「全站顶级赢家风格榜」（统计的是全站政治玩家，对世界杯跑题）；加 `PROFILES_ENABLED` 开关
- 新增 **赛果总结 + 命中率追踪**：赛前锁定「巨鲸多数方 / 最大单大户」方向，完赛后对比赛果，累计命中率（`--results` 查看）
- 持仓快照升级 **精华版**：每盘附「最大单大户」地址+方向；下方附完整英文版
- 持仓快照改为 **独立低门槛 + 按市场精确统计**；每 15 分钟推送

## 历史里程碑（V3.1 之前）
- 多赛道支持 + 世界杯频道（@polarisresearch2000_PE），24/7 云端（GitHub Actions）
- 繁体中文双语 + 市场标题规则翻译（球队港式译名）
- 每赛道独立门槛 + 体育噪音过滤（比分/角球/prop）；单轮限量防刷屏
- 巨鲸雷达：实时信号（聪明钱方向性下注）+ 常胜钱包观察名单
- 加密频道（@polarisresearch2000）+ Telegram 推送 + GitHub Actions 云端
